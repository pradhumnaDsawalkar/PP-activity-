#include <iostream>

using namespace std;
 
 
class A{
void add(int a, int b)
{

  cout << "sum = " << (a + b);
}
 

void add(double a, double b)
{

    cout << endl << "sum = " << (a + b);
}
 };


int main()
{
A a1,b1;
    a1.add(10, 2);

    b1.add(5.3, 6.2);
 

    return 0;
}
